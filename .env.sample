# Copy to .env and fill in. .env is gitignored; never commit real credentials.
# Paper trading keys only — this project has no reason to hold live-trading credentials.
ALPACA_KEY_ID=
ALPACA_SECRET_KEY=
